{
  "name": "server",
  "scripts": {
    "start": "json-server --watch db.json --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
